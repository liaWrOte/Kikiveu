import React from 'react';

import './index.scss';

const Header = () => (
  <header className="header">
    <p>Messages</p>
    <img alt="logo KikiVeu" />
    <p>Déconnexion</p>
  </header>
);

export default Header;
